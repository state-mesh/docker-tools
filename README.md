# StateMesh & Appliance Docker images

### Usage examples

* Build axolotl-ray image
```shell
make build REPO=axolotl-ray
```

* Build and release axolotl-ray image
```shell
make release REPO=axolotl-ray
```